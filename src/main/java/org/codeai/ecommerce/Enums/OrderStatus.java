package org.codeai.ecommerce.Enums;

public enum OrderStatus {
        PENDING,
        DELIVERED,
        CANCELLED,
        RETURNED,
        REFUNDED,
        SHIPPED,
        PROCESSING,
        COMPLETED,
        FAILED,
        DECLINED,
        VOIDED,
        AUTHORIZED,
        AWAITING_PAYMENT,
        AWAITING_FULFILLMENT,
        AWAITING_PICKUP,
        AWAITING_SHIPMENT,
        AWAITING_PROCESSING,
        AWAITING_DELIVERY,
        AWAITING_COLLECTION,
        AWAITING_CONFIRMATION,
        AWAITING_RELEASE,

}
